import 'package:flutter/material.dart';

class AppColors {
  // Light mode colors
  static const lightPrimary = Color(0xFF4A90E2);
  static const lightSecondary = Color.fromRGBO(255, 159, 28, 1);
  static const lightBackground = Color(0xFFF7F9FC);
  static const lightText = Color(0xFF1A1A1A);
  static const lightAccent = Color.fromRGBO(255, 159, 28, 1);
  // Dark mode colors
  static const darkPrimary = Color(0xFF357ABD);
  static const darkSecondary = Color.fromRGBO(255, 159, 28, 1);
  static const darkBackground = Color(0xFF121212);
  static const darkText = Color(0xFFFFFFFF);
  static const darkAccent = Color.fromRGBO(255, 159, 28, 1);
  // Semantics
  static const successText = Color.fromRGBO(255, 159, 28, 1);
  static const successBackground = Color.fromRGBO(255, 159, 28, 1);

  static const errorText = Color.fromRGBO(255, 159, 28, 1);
  static const errorBackground = Color.fromRGBO(255, 159, 28, 1);
}
