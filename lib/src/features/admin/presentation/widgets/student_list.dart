import 'package:flutter/material.dart';

class AdminStudentCard extends StatelessWidget {
  const AdminStudentCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
